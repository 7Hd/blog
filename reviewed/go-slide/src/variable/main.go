package main

import (
	"fmt"
)

func main() {
	// Declare the variable “name”.
	var name string // HL

	// Assign a string value to it.
	name = "Michael"

	hello := "Hello" // HL

	// Call the variable.
	fmt.Println(hello, name)
}
